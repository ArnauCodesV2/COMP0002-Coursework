#include "globals.h"
 
int columns = 15;
int rows = 15; 
int markerCol = 13; 
int markerRow = 13; //remember the grid is (r-2)x(c-2) so (13,13) is the bottom right of the grid in this case
int colStart = 1; 
int rowStart = 1; 
int maxX = 500;
int maxY = 300;
//Dimensions are assumed x : 0 -> 500 and y: 0 -> 300